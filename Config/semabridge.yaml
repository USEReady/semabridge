project_name: "sales"
source:
  type: fabric
  model: "*"
targets:
  - type: snowflake
ui:
  intermediate_format: "osi"
  editor_mode: "form"
options:
  auto_relationships: true
  generate_descriptions: true