# Fabric compatibility mode

snowflake:
  quote_identifiers: true
  use_transient_tables: true

fabric:
  tmsl_generation_mode: "compatibility"

semantic_model:
  view_suffix: ""

compatibility:
  force_uppercase: false
