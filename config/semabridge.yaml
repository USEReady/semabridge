project_name: b
source:
  type: snowflake
  database: SEMABRIDGE_DB
  schema: PUBLIC
  models:
    - CORE_FINANCE_V1_SEMANTIC
targets:
  - type: fabric
mappings:
  - source: CORE_FINANCE_V1_SEMANTIC
    target: core_finance_v1_semantic_mapped
    type: table
    columns:
      - source: id
        target: id
        type: INT
        primary_key: true
      - source: name
        target: name
        type: VARCHAR(255)
      - source: created_at
        target: created_at
        type: TIMESTAMP
      - source: updated_at
        target: updated_at
        type: TIMESTAMP
ui:
  intermediate_format: osi
  editor_mode: form
selection:
  model_ids:
    - CORE_FINANCE_V1_SEMANTIC
options:
  auto_relationships: true
  include_hidden_fields: false
  generate_descriptions: true