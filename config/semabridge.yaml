project_name: SN-FABRIC
source:
  type: snowflake
  database: SEMABRIDGE_DB
  schema: PUBLIC
  models:
    - CONTINENT_semantic
targets:
  - type: fabric
mappings:
  - source: CONTINENT_semantic
    target: continent_semantic_mapped
    type: table
    columns:
      - source: id
        target: id
        type: INT
        primary_key: true
      - source: name
        target: name
        type: VARCHAR(255)
      - source: created_at
        target: created_at
        type: TIMESTAMP
      - source: updated_at
        target: updated_at
        type: TIMESTAMP
ui:
  intermediate_format: osi
  editor_mode: yaml
selection:
  model_ids:
    - CONTINENT_semantic
options:
  auto_relationships: true
  include_hidden_fields: false
  generate_descriptions: true