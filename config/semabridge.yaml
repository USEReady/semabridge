project_name: mma
source:
  type: snowflake
  database: ""
  schema: ""
  model: CLIENT_DATA_SEMANTIC
targets:
  - type: fabric
mappings:
  - source: CLIENT_DATA_SEMANTIC
    target: client_data_semantic_mapped
    type: table
    columns:
      - source: id
        target: id
        type: INT
        primary_key: true
      - source: name
        target: name
        type: VARCHAR(255)
      - source: created_at
        target: created_at
        type: TIMESTAMP
      - source: updated_at
        target: updated_at
        type: TIMESTAMP
ui:
  intermediate_format: osi
  editor_mode: form
  output_format: osi
selection:
  model_ids:
    - CLIENT_DATA_SEMANTIC
options:
  auto_relationships: true
  generate_descriptions: true
target:
  type: fabric